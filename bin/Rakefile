require "rake/clean"

require "thrusting"

task :build => ["gpu_sm.bin"]
  
file "gpu_sm.bin" => "gpu_sm.cu" do |t|
  include Thrusting
  cxx = nvcc_config("nvcc")
  sh "#{cxx} -o #{t.name} #{t.prerequisites[0]}"
end

CLOBBER.include("gpu_sm.bin")
